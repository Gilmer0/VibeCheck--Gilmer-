# VibeCheck 411L - Activity #3

A simple full-stack web application consisting of a Node.js Express API and a vanilla JavaScript frontend. This project demonstrates basic API routing, state management (smash counter), and a GitHub collaboration workflow.

## 🛠️ Project Structure
- `backend/`: Express server logic and API routes.
- `frontend/`: HTML and client-side JavaScript.

## 🚀 How to Run

### 1. Start the Backend
Navigate to the backend folder, install dependencies, and start the server:
```bash
cd backend
npm install
node index.js